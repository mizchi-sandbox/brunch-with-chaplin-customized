View     = require 'views/base/view'
template = require 'templates/{{name}}/{{name}}'

module.exports = class {{#camelize}}{{name}}{{/camelize}}View extends View
  container: "#scene-root"
  template: template
